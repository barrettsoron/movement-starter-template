# Example environment variables for Movement Starter Template
# Copy to .env.local and edit values as needed
#
# IMPORTANT: Only PUBLIC_ prefixed variables are exposed to the browser
# Never put secrets in PUBLIC_ variables - use server-only variables instead

# =============================================================================
# SERVER-SIDE ENVIRONMENT VARIABLES (NOT exposed to browser)
# =============================================================================

# Node environment (set automatically in most deployments)
NODE_ENV=development

# Site URL for server-side absolute URL generation (optional but recommended)
# SITE_URL=https://yoursite.com

# Movement/Campaign features that need server-side secrets
ENABLE_FORMS=false
# HCAPTCHA_SECRET=your_secret_key_here  # Required only if ENABLE_FORMS=true

# Email service secrets (server-only)
# MAILCHIMP_API_KEY=your_mailchimp_secret
# CONVERTKIT_API_SECRET=your_convertkit_secret

# Payment processing secrets (server-only)
# STRIPE_SECRET_KEY=sk_live_or_test_key
# PAYPAL_SECRET=your_paypal_secret

# =============================================================================
# PUBLIC ENVIRONMENT VARIABLES (exposed to browser)
# =============================================================================

# Base configuration - URLs for different environments
PUBLIC_BASE_URL=http://localhost:4321
# PUBLIC_PRODUCTION_URL=https://yoursite.com
# PUBLIC_STAGING_URL=https://staging--yoursite.netlify.app

# Site metadata and branding
PUBLIC_SITE_NAME=Movement Site
PUBLIC_SITE_TAGLINE=Building power for change
PUBLIC_SITE_DESCRIPTION=A progressive movement site focused on organizing and action
PUBLIC_AUTHOR_NAME=Your Organization

# Contact and social media
# PUBLIC_CONTACT_EMAIL=hello@yoursite.com
# PUBLIC_TWITTER_HANDLE=yourmovementhandle

# Feature flags - control which sections appear (set to 'false' to disable)
# All features are enabled by default for movement sites
PUBLIC_FEATURE_UPDATES=true
PUBLIC_FEATURE_STORIES=true
PUBLIC_FEATURE_EVENTS=true
PUBLIC_FEATURE_DARKMODE=true
PUBLIC_FEATURE_NEWSLETTER=true
PUBLIC_FEATURE_DONATIONS=true

# Campaign/Movement specific actions
PUBLIC_PRIMARY_ACTION=Take Action
PUBLIC_PRIMARY_ACTION_URL=/action
PUBLIC_DONATE_URL=/donate

# Analytics configuration (public write keys only, never secrets)
PUBLIC_ANALYTICS=none
# PUBLIC_ANALYTICS_DOMAIN=yoursite.com          # Required for Plausible
# PUBLIC_ANALYTICS_WRITE_KEY=your_public_key    # Generic analytics key
# PUBLIC_GOOGLE_ANALYTICS_ID=G-XXXXXXXXXX      # Required for Google Analytics

# Environment detection (set automatically by deployment platforms)
# PUBLIC_ENVIRONMENT=development
# PUBLIC_DEPLOY_CONTEXT=branch-deploy

# =============================================================================
# VALIDATION NOTES
# =============================================================================
#
# This template uses Zod for environment validation. The build will fail if:
# - Required variables are missing or invalid
# - HCAPTCHA_SECRET is missing when ENABLE_FORMS=true
# - Analytics configuration is incomplete for the selected provider
# - Production URLs don't use HTTPS
# - Email addresses are malformed
#
# Run 'npm run check:env' to validate your configuration
# Run 'npm run build:unsafe' to skip validation (not recommended)
EOF < /dev/null